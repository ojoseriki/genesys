<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.st.genesys">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homepage" />
        <activity android:name=".DecidePage" />
        <activity android:name=".companySignup" />
        <activity android:name=".employeeSignup" />
        <activity android:name=".goodsPage" />
        <activity android:name=".productivityPage" />
        <activity android:name=".servicePage" />
        <activity android:name=".myAccoutPage" />
        <activity android:name=".GraphActivity"></activity>
    </application>

</manifest>